<UserControl x:Class="DarbWareERP.控制項.導覽區DataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DarbWareERP.控制項"
             xmlns:src="clr-namespace:DarbWareERP.繼承窗口"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" x:Name="dd">
    <UserControl.Resources>
        <Style x:Key="datagridstyle" TargetType="DataGrid">
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Visible"></Setter>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Visible"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <DataGrid x:Name="dataGrid"  ItemsSource="{Binding}" Style="{StaticResource datagridstyle}" ColumnWidth="SIZETOHEADER" AutoGeneratedColumns="dataGrid_AutoGeneratedColumns" IsReadOnly="True" />
        <Button x:Name="button1"  HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="19" Height="19" Click="button1_Click" >
            <Image Source="/DarbWareERP;component/Image/Icon_png/gridmemory.bmp"  HorizontalAlignment="Center" VerticalAlignment="Center" UseLayoutRounding="False" Stretch="UniformToFill" Width="17" Height="17"/>
        </Button>
    </Grid>
</UserControl>
