<UserControl x:Class="DarbWareERP.控制項.瀏覽條件區"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DarbWareERP.控制項"
             mc:Ignorable="d" 
             d:DesignHeight="210" d:DesignWidth="490" IsTabStop="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO"></RowDefinition>
            <RowDefinition Height="182*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="AUTO"></ColumnDefinition>
                <ColumnDefinition Width="AUTO"></ColumnDefinition>
                <ColumnDefinition Width="AUTO"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="txtbl預設條件" Text="預設條件" Grid.Column="0" Margin="3,4,0,0" FontSize="18.667" Foreground="#FF313AE8"/>
            <ComboBox x:Name="comboBox" Grid.Column="1" Margin="3,6,0,0"  Width="120"/>
            <TextBox x:Name="textBox" Grid.Column="2" Margin="3,6,0,0"  Width="240"/>

        </Grid>

        <DataGrid x:Name="dataGrid" Margin="3" Grid.Row="1"/>

    </Grid>
</UserControl>
